<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zsx.com.aiyamaya">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ui.activity.GuideActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.HomeActivity"></activity>
        <activity android:name=".ui.activity.SettingActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.ForgetPassActivity" />
        <activity android:name=".ui.activity.UpdatePassActivity" />
        <activity android:name=".ui.activity.ArticleActivity" />
        <activity android:name=".ui.activity.CommunicateActivity" />
        <activity android:name=".ui.activity.PostDetailActivity" />
        <activity android:name=".ui.activity.WritePostActivity"></activity>
    </application>

</manifest>